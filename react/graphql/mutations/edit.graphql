mutation edit($editMenu: MenuInput!) {
  editMenu(menuInput: $editMenu) {
    id
    name
    icon
    slug
    styles
    display
    enableSty
    order
    menu {
      id
      name
      icon
      slug
      styles
      display
      enableSty
      order
      menu {
        id
        name
        icon
        slug
        styles
        display
        enableSty
        order
      }
    }
  }
}
