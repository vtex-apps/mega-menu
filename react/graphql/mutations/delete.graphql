mutation delete($id: String!, $idSecond: String, $idThird: String) {
  deleteMenu(id: $id, idSecond: $idSecond, idThird: $idThird) {
    id
    name
    icon
    slug
    styles
    display
    enableSty
    order
    menu {
      id
      name
      icon
      slug
      styles
      display
      enableSty
      order
      menu {
        id
        name
        icon
        slug
        styles
        display
        enableSty
        order
      }
    }
  }
}
