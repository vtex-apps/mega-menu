query getMenu($id: ID!) {
  menu(id: $id) {
    id
    name
    icon
    slug
    styles
    display
    enableSty
    order
    menu {
      id
      name
      icon
      slug
      styles
      display
      enableSty
      order
      menu {
        id
        name
        icon
        slug
        styles
        display
        enableSty
        order
      }
    }
  }
}
