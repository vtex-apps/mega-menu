query getMenu($id: ID!) {
  menu(id: $id) {
    id
    name
    icon
    slug
    binding
    styles
    display
    enableSty
    order
    menu {
      id
      name
      icon
      slug
      styles
      display
      enableSty
      order
      slugRoot
      slugRelative
      menu {
        id
        name
        icon
        slug
        styles
        display
        enableSty
        order
        slugRoot
        slugRelative
      }
    }
  }
}
