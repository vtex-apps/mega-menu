query getMenu($id: ID!) {
  menu(id: $id) {
    id
    name
    icon
    uploadedIcon
    slug
    styles
    display
    enableSty
    order
    banner
    menu {
      id
      name
      icon
      uploadedIcon
      slug
      styles
      display
      enableSty
      order
      slugRoot
      slugRelative
      menu {
        id
        name
        icon
        slug
        styles
        display
        enableSty
        order
        slugRoot
        slugRelative
        optionalText
      }
    }
  }
}
